# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/mtg_nexus?schema=public"

# JWT Configuration
JWT_SECRET="tu_secreto_jwt_super_seguro_cambiar_en_produccion"
JWT_EXPIRES_IN="7d"

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
# En desarrollo: acepta localhost en cualquier puerto (3000, 3001, 3002, etc)
# En producción: usar solo el origen específico
CORS_ORIGIN="http://localhost:3000"

# Scryfall API (no requiere API key, pero se puede configurar rate limiting)
SCRYFALL_API_BASE_URL="https://api.scryfall.com"
